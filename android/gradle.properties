org.gradle.jvmargs=-Xmx4096M
android.useAndroidX=true
# Kept false: flutter_local_notifications still applies the legacy kotlin-android
# plugin, which is incompatible with AGP 9 built-in Kotlin. Flip to true once
# all plugins migrate (see migrate-to-built-in-kotlin guide).
android.builtInKotlin=false
android.newDsl=false
